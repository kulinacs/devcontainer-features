{
  "id": "cloudflared",
  "version": "1.0.0",
  "name": "cloudflared",
  "documentationURL": "https://github.com/kulinacs/devcontainer-features/tree/main/src/cloudflared",
  "description": "Cloudflare Tunnel client",
  "options": {},
  "customizations": {
    "vscode": {
      "settings": {
        "github.copilot.chat.codeGeneration.instructions": [
          {
            "text": "This dev container includes cloudflared (`cloudflared`) pre-installed and available on the `PATH`."
          }
        ]
      }
    }
  },
  "dependsOn": {}
}
